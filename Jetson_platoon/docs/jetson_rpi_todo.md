# Jetson/RPi5 측 필요 작업 정리 (STM32 코드 기준)

STM32 레포(`Platoon_STM32`)의 실제 구현 상태와 Jetson 레포(`Platoon-Platoon1/Jetson_platoon`)의
현재 코드(`main.py`, `driving_control.py`, `platoon_fsm.py`, `stm32_interface.py`)를 맞대본 결과입니다.
STM32 쪽 프로토콜/엔코더/PID 재작업이 끝나는 것과 별개로, **Jetson 쪽에서도 손볼 부분**이 있습니다.

> **설계 변경 반영 (중요)**
> 이전에는 "플래툰 중 팔로워는 카메라를 끄고 V2V/UWB만으로 주행"하는 구조였으나,
> **라인트레이싱을 하면서 동시에 V2V로 군집주행**하는 구조로 변경되었습니다.
> - **횡방향(조향)** = 카메라 라인트레이싱 — 리더·팔로워·단독주행 전부 동일
> - **종방향(속도·차간거리)** = V2V + UWB — 플래툰 중에만
>
> 이 변경으로 §1.1(초음파 활용)과 §2(조향 관련 상수)의 판단 근거가 일부 달라졌으니
> 아래 본문을 참고하세요.

---

## 1. 코드 갭 — 값은 오는데 안 쓰고 있음

### 1.1 초음파 거리(`front_distance`) 값을 전혀 사용하지 않음

`stm32_interface.py`의 `DriveFeedback.front_distance`는 STM32가 올려주는 초음파 실측 거리(m)를
담는 필드입니다. STM32 쪽 코드(`supersonic.c`, `PB6 ECHO` + `TIM4` 입력캡처)를 보면 초음파 센서는
**STM32에 직결**되어 있고, `stm32_protocol.md`에도 "PLATOON_MAINTAIN 단계의 주 거리 센서는 초음파
(UWB는 보조)"라고 명시돼 있습니다.

그런데 지금 `main.py`는 `feedback.front_distance`를 **전혀 읽지 않고**, `feedback.obstacle`
(불리언 플래그)만 씁니다. 거리 제어는 전부 ESP32의 UWB 값(`partner.uwb_distance`)만 사용 중입니다.

→ **팀 결정 필요**: 설계문서대로 초음파를 MAINTAIN 단계의 주 거리값으로 실제 제어 루프에
반영할지, 아니면 지금처럼 UWB를 주로 쓰고 초음파는 근접 안전정지(안전용 backup)로만 남길지
정해야 합니다. 반영하기로 하면 `main.py`에서 `feedback.front_distance`를 `compute_control()`
호출부로 연결하는 코드가 필요합니다.

> **참고 — 두 센서의 성질이 다릅니다**
> - **초음파**: 앞에 있는 게 무엇이든 감지. 단, "그게 리더인지 낙하물인지" 구분 불가
> - **UWB**: 리더가 확실하지만, V2X 미장착 차량·장애물은 완전히 안 보임
>
> 즉 둘은 대체재가 아니라 보완재입니다. 실제로 **리더와 팔로워 사이에 뭔가 끼어들면
> 초음파만 그걸 볼 수 있습니다.** 그래서 "UWB=거리 제어 / 초음파=안전 감시"로 역할을
> 나누는 쪽을 권장합니다(둘 중 하나만 쓰는 게 아니라).

### 1.2 `feedback.failsafe` 를 아예 안 읽음

STM32가 RPi 무응답(200ms 초과)을 감지하면 `STATUS_FAILSAFE` 비트를 세워 보내주는데,
`main.py`는 `feedback.obstacle`만 읽고 `feedback.failsafe`는 코드 어디에서도 참조하지 않습니다.

STM32는 자체적으로 정지하지만, Jetson의 FSM/제어 로직은 이 사실을 모른 채 계속 목표 속도를
계산해서 내려보낼 수 있습니다(차가 멈췄는데 FSM은 "주행 중"으로 착각). 최소한 로그를 남기거나,
`failsafe`가 True인 동안은 FSM을 안전 상태로 전이시키는 처리를 추가하는 게 좋습니다.

> **플래툰 중이면 더 중요합니다.** 자차가 STM32 failsafe로 멈췄는데 FSM이 계속
> `PLATOON_MAINTAIN`을 유지하면, 뒤따르는 팔로워는 "리더가 정상 주행 중"으로 알고
> 0.8m 간격을 유지하려 접근합니다. failsafe 감지 시 `EMERGENCY` 패킷을 플래툰에
> 전파하는 경로가 필요합니다(§1.4 참고).

### 1.3 `EgoState.obstacle` 필드가 선언만 되고 로직에서 안 쓰임

`platoon_fsm.py`에 `obstacle: bool = False` 필드는 있지만, FSM의 어떤 판단 로직도 이 값을
참조하지 않습니다(감속, 정지, 상태 전이 등 어디에도 안 걸림). `main.py`에서 값은 채워주고
있으니(`ego.obstacle = feedback.obstacle`), FSM 쪽에 "장애물 감지 시 어떻게 반응할지"
로직을 추가해야 실제로 동작합니다.

### 1.4 `EMERGENCY` 패킷 **송신 경로가 없음** (추가)

`v2x_protocol.py`에 `EmergencyMessage`가 정의돼 있고, `platoon_fsm.py`에 수신 처리
(`_on_emergency`)도 구현돼 있습니다. 그런데 **보내는 코드가 어디에도 없습니다.**

즉 현재는 다음 상황에서 뒤차에게 아무 경고도 못 보냅니다.

- 초음파로 전방 장애물 감지 (§1.1)
- STM32 failsafe 발동 (§1.2)
- 라인 유실로 비상정지 (§1.5)

§1.1~1.3을 구현할 때 이 송신 경로를 같이 넣어야 의미가 있습니다.

### 1.5 라인 유실 시 동작 — STM32와 맞물리는 부분 (추가)

`driving_control.py`에 라인 유실 처리가 들어가 있고, **플래툰 여부에 따라 동작이 갈립니다.**

| 상황 | 동작 | STM32로 나가는 모드 |
|---|---|---|
| 단독주행 + 라인 유실 0.3초 초과 | 정지 | `MODE_EMERGENCY_STOP` (0xFF) |
| 플래툰 중 + 라인 유실 | UWB로 앞차 추종하며 버팀 | 평상 모드 유지 |

플래툰 중에는 뒤에 팔로워가 0.8m로 붙어 있어 급정지가 더 위험하므로, 라인을 놓쳐도
바로 멈추지 않고 UWB pure pursuit로 버티도록 되어 있습니다(`ControlOutput.uwb_fallback`).

→ **STM32 확인 필요**: `MODE_EMERGENCY_STOP`(0xFF)을 받았을 때 목표속도와 무관하게
**즉시 제동**하는 처리가 구현되어 있는지 확인해주세요. 평상시 PID 감속으로 처리하면
라인 이탈 상황에서 제동거리가 길어집니다.

### 1.6 라인트레이싱 연동부가 아직 스텁 (추가, **최우선**)

`main.py`의 `read_lane_tracing()`이 현재 고정값(`offset=0.0, detected=False`)을 반환하는
스텁입니다. 라인트레이싱 담당자의 코드와 연결되기 전까지는 **차가 실제로 주행하지 못합니다.**

설계 변경으로 조향이 전부 카메라 기반이 되면서, 이제 이 연결이 전체 시스템의 병목입니다.
라인트레이싱 쪽에서 넘겨받아야 하는 값:

| 값 | 형식 | 용도 |
|---|---|---|
| `offset` | 정규화 `-1.0 ~ +1.0`<br>`(라인중심_x - 이미지중심_x) / (이미지폭/2)` | 조향 PID 입력, 차선 정렬 판정 |
| `detected` | `bool` | 라인 유실 감지 |

> 픽셀값 그대로가 아니라 **정규화해서** 받아야 해상도·ROI가 바뀌어도 PID 게인을
> 다시 잡지 않습니다.

`EgoState.lane_offset` / `EgoState.lane_detected`에 이 값이 채워져야 JOIN 단계의
차선 정렬 판정(`_lane_align_complete`, 설계문서 §19.4)이 동작합니다.

### 1.7 `EgoState`의 체크포인트/경로가 비어 있음 (추가)

`EgoState.checkpoint`, `EgoState.route`를 아무도 채우지 않고 있습니다. 이 값들은
플래툰 적합도 계산에서 **경로 중첩 점수(가중치 0.35, 5개 항목 중 최대)** 와
EXIT 진입 판정(§21.1)에 쓰입니다.

현재는 팀 결정에 따라 **보류 상태**이며, 이 상태로는 결합 판단의 정확도가 떨어집니다.
체크포인트를 무엇으로 인식할지(표지판 / 바닥 마커 / 주행거리 누적) 결정이 필요합니다.

---

## 2. STM32와 반드시 값을 맞춰야 하는 상수

| 파라미터 | Jetson 현재값 | STM32 실제값/제약 | 확인할 것 |
|---|---|---|---|
| `MAX_STEERING_RAD` (`driving_control.py`) | 30° (임시) | 서보 하드웨어 범위 -45°~+45° (`servo.h`, 1000~2000µs) | 30°가 의도된 마진인지, 45°로 맞출지 팀 결정. 어느 쪽이든 STM32 서보 설정과 **동일 값**이어야 함 |
| `WHEELBASE_M` (`driving_control.py`) | 0.20 (임시) | — (기구 설계값) | 줄자 실측 필요. **단, 설계 변경으로 영향 범위 축소** — 이제 pure pursuit는 "플래툰 중 라인 유실" 폴백에서만 쓰임(§1.5). 여전히 실측은 필요하나 우선순위는 낮아짐 |
| 시리얼 포트 (`main.py` `open_stm32_port()`) | `/dev/ttyAMA0` 하드코딩 | STM32는 `USART2` 고정 | RPi5 실제 배선(GPIO UART vs USB-시리얼 어댑터) 확인 후 포트 경로 확정 |
| Baudrate | 115200 | 115200 (`serial_communication.c` 기준) | 현재는 일치. 패킷 유실 잦으면 460800으로 **양쪽 동시에** 올려야 함 |
| 조향 부호 | 좌회전 = 양수 (변경 없음) | STM32에서 뒤집기로 합의됨 (`stm32_protocol.md` §5.2) | Jetson 쪽 코드 수정은 불필요. 실차에서 좌/우 반대로 도는지 **검증만** 필요 |
| `MODE_EMERGENCY_STOP` 처리 | 라인 유실 시 송신함 (§1.5) | 확인 필요 | STM32가 0xFF 수신 시 즉시 제동하는지 |

---

## 3. STM32가 아직 완성 전이라 생기는 임시 이슈

STM32 쪽은 현재 구프로토콜(ASCII, `PWM:...`/`M:...`)을 쓰고 있고, `stm32_protocol.md`가 정의한
바이너리 프로토콜 + 엔코더/PID 폐루프 제어는 아직 미구현 상태입니다 (별도로 정리한 STM32 작업
목록 참고). 이게 끝나기 전까지 Jetson 쪽에서 알아둘 점:

- **`stm32_interface.py`는 이미 바이너리 프로토콜로 완성돼 있어서**, STM32가 프로토콜을 갈아엎기
  전까지는 실제 보드 간 통신 테스트 자체가 안 됩니다. Jetson 쪽은 추가 구현 없이 대기하면 되고,
  STM32 교체 완료 후 통합 테스트만 진행하면 됩니다.
- **`current_speed` 피드백 신뢰 불가**: STM32에 실제 엔코더 입력이 없어서(현재 `encoder.c`는
  개루프 PWM 제어만 함) `current_speed_mmps`를 채울 방법이 없습니다. `main.py`는 이미
  `feedback is None`이면 이전 속도값을 유지하는 fallback이 있는데, 엔코더가 붙기 전까지는
  이 fallback 경로가 계속 타게 됩니다 — 통합 테스트 때 이 부분이 정상 동작인지 헷갈리지 않도록
  인지하고 있어야 합니다.
- **엔코더 없으면 플래툰 제어가 성립하지 않습니다** (추가). 팔로워 목표속도 계산식
  `v_target = v_L + K_d(d - d_target) + K_v(v_L - v_F)` 에서 `v_F`(자차 실제 속도)가
  엔코더 값입니다. 이게 없으면 결합·유지 제어 자체를 검증할 수 없으므로,
  **엔코더 구현이 플래툰 실차 시험의 선행 조건**입니다.

---

## 4. 팀에서 "결정"만 하면 되는 항목 (측정 대상 아님)

`parameters.md` §7에 정리된 것 중 코드/문서에 직접 걸리는 것들:

- `vehicle_id` — `main.py`의 하드코딩된 `1`을 차량별 고유 ID 부여 방식으로 교체
- `reason_code` 체계 — `v2x_protocol.py`의 `PlatoonJoinReject` 거절 사유 코드 정의
  (현재 5종 정의됨: 참여 비허용 / 이미 결합 중 / 절대조건 미달 / 적합도 미달 / 차량 미발견)
- `CHECKPOINT_ADJACENT_RANGE`, `lane_ok`(현재 항상 True) — 체크포인트/트랙 차선 구조 확정 후 값 결정
- `LANE_ALIGN_OFFSET_TOLERANCE` (0.15, 정규화 오프셋 기준) — 설계 변경으로 **UWB 횡방향
  이격거리에서 카메라 오프셋 기준으로 되돌아감**(설계문서 §19.4 원안). 이전 문서의
  `LANE_ALIGN_LATERAL_TOLERANCE_M`은 더 이상 존재하지 않는 상수입니다.
- **트랙 차선 수** (추가) — 단일 차선이면 차선 변경 로직 자체가 불필요해집니다.
  다차선이면 JOIN 중 차선 변경 시 **옆 차선 점유 여부를 판단할 센서가 현재 없다**는
  문제가 남습니다(측면·후측방 센서 미탑재). 설계문서 §24에도 "JOIN 중 차선 변경 실패"가
  미해결 항목으로 남아 있습니다.

---

## 5. 통합 테스트 체크리스트 (STM32 업데이트 완료 후)

1. 루프백 — STM32가 보낸 피드백을 `stm32_interface.py`의 `PacketParser`가 정상 파싱하는지
2. 체크섬 오류 주입 — 일부러 깨진 프레임을 보내고 양쪽 다 조용히 폐기하는지
3. 케이블 중간 절단/재연결 — 헤더 재탐색으로 정상 복구되는지
4. Failsafe — Jetson 쪽 송신을 멈추고 200ms 후 STM32가 실제로 멈추는지, 그리고 이때
   `feedback.failsafe`를 Jetson이 인지하는지 (§1.2 구현 후)
5. 초음파 값 반영 여부 결정 후 — MAINTAIN 상태에서 초음파/UWB 중 실제로 쓰기로 한 값이
   거리 제어에 제대로 반영되는지
6. **비상정지** (추가) — 라인트레이싱 연결 후, 라인을 가려서 0.3초 이상 유실시켰을 때
   `MODE_EMERGENCY_STOP`이 나가고 STM32가 **즉시** 제동하는지 (§1.5)
7. **조향 범위 클램프** (추가) — 급커브에서 `MAX_STEERING_RAD`를 넘는 명령이 나갈 때
   서보 물리 한계를 넘지 않는지. Jetson·STM32 양쪽 클램프 값이 일치하는지
8. **엔코더 속도 정합성** (추가) — 목표속도 0.5m/s를 내렸을 때 `current_speed`가
   실제로 그 근처로 수렴하는지. 여기서 오차가 크면 플래툰 거리 제어가 전부 틀어짐

---

## 6. 작업 우선순위 (추가)

의존 관계를 고려한 순서입니다.

| 순위 | 작업 | 담당 | 막고 있는 것 |
|---|---|---|---|
| 1 | 라인트레이싱 연동 (§1.6) | 라인트레이싱 + Jetson | **주행 자체가 불가** |
| 2 | STM32 바이너리 프로토콜 교체 | STM32 | 보드 간 통신 테스트 전부 |
| 3 | 엔코더 + 속도 PID (§3) | STM32 | 플래툰 거리 제어 검증 |
| 4 | 초음파 활용 방식 결정 (§1.1) | 팀 | 안전 로직 |
| 5 | 장애물/failsafe 대응 + EMERGENCY 송신 (§1.2~1.4) | Jetson | 안전 로직 |
| 6 | 실측 파라미터 확정 (`parameters.md`) | 팀 | 실차 시험 |
| 7 | 체크포인트/경로 (§1.7) | 팀 | 결합 판단 정확도 |

1번과 2번은 서로 독립이라 **병렬 진행 가능**합니다.
